# Copy to .env.local. Read at BUILD time only — the site is a static export.

# Public URL of the Atelier Omar API (../backend).
# Local: http://localhost:4400 — production: https://maisondeco.ma (API derriere le meme domaine)
NEXT_PUBLIC_API_URL=http://localhost:4400
